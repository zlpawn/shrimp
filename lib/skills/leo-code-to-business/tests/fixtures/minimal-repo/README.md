# Minimal Repository Fixture

This directory documents the smallest repository shape used by snapshot and guard tests. Individual
tests create isolated Git repositories dynamically so commit IDs and dirty-worktree behavior remain
deterministic.
